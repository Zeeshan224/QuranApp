<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewStatusSaver" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/blue</item>
    </style>

    <!-- Splash screen theme -->
    <style name="SplashScreenTheme" parent="Theme.SplashScreen">
        <!-- Set your splash screen background -->
        <item name="windowSplashScreenBackground">@color/blue</item>

        <!-- Set your splash screen icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>

        <!-- Control the animation duration -->
        <item name="windowSplashScreenAnimationDuration">300</item>
    </style>
    <style name="Theme.NewStatusSaver" parent="Base.Theme.NewStatusSaver" />
</resources>